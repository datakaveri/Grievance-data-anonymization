services:
  anonymizer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: grievance_anonymizer
    environment:
      - HF_TOKEN=${HF_TOKEN:-}
    volumes:
      - ./config:/app/config:ro
      - ./data:/app/data
      - ./work:/app/work
      - ./output:/app/output
    # Enable NVIDIA GPU support if docker container toolkit is available:
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]
