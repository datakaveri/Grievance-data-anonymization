# Core ML & Transformers Stack
torch>=2.0.0
transformers>=4.40.0
huggingface-hub>=0.22.0
accelerate>=0.26.0
sentencepiece
protobuf<6.0.0,>=3.20.2

# Document Readers & Parsers
python-docx
lxml  # not imported directly; python-docx needs it to parse .docx XML
beautifulsoup4

# NLP & PII Redaction Engines
spacy>=3.7.0
presidio-analyzer>=2.2.0

# Text Processing & Utilities
pandas>=2.0.0,<3.0.0
openpyxl>=3.1.0
xlrd>=2.0.1  # not imported directly; pandas needs it to read legacy .xls files
tqdm
colorama
